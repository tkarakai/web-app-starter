# Next.js public base URL used by BetterAuth.
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Convex deployment URL (set after running `bunx convex dev`).
NEXT_PUBLIC_CONVEX_URL=
CONVEX_URL=

# BetterAuth secrets (change for production).
BETTER_AUTH_SECRET=
