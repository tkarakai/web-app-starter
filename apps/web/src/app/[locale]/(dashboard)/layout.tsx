// Platform route (validates the session and enforces access rules for every signed-in page); the logic lives in @web-app-starter/auth-ui.
// To customise it, replace this re-export with your own component.
export { ProtectedLayout as default } from "@web-app-starter/auth-ui/views";
