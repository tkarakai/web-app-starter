/**
 * Verification links from Better Auth should always land on a status page
 * instead of redirecting directly into the app.
 */
export const EMAIL_VERIFICATION_CALLBACK_URL = "/verify-email?verified=1";
